name: family_social
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.17.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  # Add your other dependencies here
  firebase_core: ^2.5.0
  firebase_auth: ^4.1.0
  cloud_firestore: ^4.17.4 # Ensure this is listed only once
  sign_in_button: ^3.2.0
  sqflite: ^2.0.0+3
  path: ^1.8.1
  universal_io: ^2.0.4
  google_sign_in: ^6.2.1

flutter:
  uses-material-design: true


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  cupertino_icons: ^1.0.2


assets:
    - assets/welcome.png
    - assets/google.png
    - assets/g_logo.png
